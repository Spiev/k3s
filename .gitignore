# Lokale Overlays mit echten Domains und privaten Werten — nicht ins Repo
apps/*/overlays/*/ingress-patch.yaml
apps/*/**-ingress.yaml

# Sensitive architecture docs — local only, not for public repo
docs/proxy-migration.md
docs/decisions/ingress-security.md
docs/decisions/uptime-monitoring.md

# Secrets and credentials — never commit
scripts/.credentials
**/.env
**/*.env
!infrastructure/k3s-version.env
