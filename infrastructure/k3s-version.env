# Pinned k3s version — Renovate tracks this for update notifications.
#
# Upgrade on the server node:
#   curl -sfL https://get.k3s.io | INSTALL_K3S_VERSION=<version> sh -
#
# Verify afterwards:
#   k3s --version
#   kubectl get nodes
#   kubectl get pods -A

K3S_VERSION=v1.35.4+k3s1
