apiVersion: v1
kind: Namespace
metadata:
  name: freshrss
  labels:
    app: freshrss
    type: service
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: freshrss-config-enc
  namespace: freshrss
  labels:
    app: freshrss
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn-retain-encrypted
  resources:
    requests:
      storage: 5Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: freshrss
  namespace: freshrss
  labels:
    app: freshrss
spec:
  replicas: 1
  selector:
    matchLabels:
      app: freshrss
  strategy:
    type: Recreate      # Recreate statt RollingUpdate: PVC ist RWO, darf nur von einem Pod gleichzeitig gemountet werden
  template:
    metadata:
      labels:
        app: freshrss
    spec:
      containers:
        - name: freshrss
          image: lscr.io/linuxserver/freshrss:1.28.1-ls301@sha256:e717cfb455202f38a61d50f5df21b421c11fabca3f288ee121cc8470323dddd3
          env:
            - name: PUID
              value: "1000"
            - name: PGID
              value: "1000"
            - name: TZ
              value: "Europe/Berlin"
          ports:
            - containerPort: 80
          volumeMounts:
            - name: config
              mountPath: /config
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 10
            periodSeconds: 10
      volumes:
        - name: config
          persistentVolumeClaim:
            claimName: freshrss-config-enc
---
apiVersion: v1
kind: Service
metadata:
  name: freshrss
  namespace: freshrss
  labels:
    app: freshrss
spec:
  selector:
    app: freshrss
  ports:
    - port: 80
      targetPort: 80
