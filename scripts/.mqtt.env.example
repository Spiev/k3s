# MQTT configuration — shared by all scripts that publish to Home Assistant.
# Copy to .mqtt.env and set permissions:
#   cp .mqtt.env.example .mqtt.env && chmod 600 .mqtt.env
MQTT_HOST="your_mqtt_broker_hostname"
MQTT_PORT="1883"
MQTT_USER="your_mqtt_username"
MQTT_PASSWORD="your_mqtt_password"
